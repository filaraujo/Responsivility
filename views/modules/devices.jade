div.devices
    each device in devices
        div.device(class="#{device.id}", data-dimensions="[#{device.dimensions}]", data-name="#{device.name}")
                p #{device.name} ( #{device.dimensions[0]} • #{device.dimensions[1]} )